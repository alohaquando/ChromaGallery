// Firebase backend is no longer live. These are unused no-op stubs kept only
// so any stray imports elsewhere don't crash the build. All real data flows
// through the in-memory mocks in $lib/data (item.ts, collection.ts, list.ts, auth.ts).
export const db = null;
export const auth = null;
export const storage = null;
